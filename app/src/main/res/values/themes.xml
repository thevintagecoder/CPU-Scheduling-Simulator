<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style
        name="Base.Theme.CPUSchedulingSimulator"
        parent="Theme.Material3.DayNight.NoActionBar">

        <item name="android:windowBackground">@color/app_background</item>
        <item name="android:statusBarColor">@color/app_background</item>
        <item name="android:navigationBarColor">@color/app_background</item>

        <item name="colorPrimary">@color/accent_cyan</item>
        <item name="colorOnPrimary">@color/accent_text</item>

        <item name="android:fontFamily">sans</item>
        <item name="android:colorAccent">@color/accent_cyan</item>

        <!-- Keep status-bar icons light because the background is dark -->
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style
        name="Theme.CPUSchedulingSimulator"
        parent="Base.Theme.CPUSchedulingSimulator" />

</resources>